'use strict' // (optional) treat all JS code as newer versions

// Documentation for JavaScript
// * https://developer.mozilla.org/en-US/docs/Web/JavaScript

// Documentation for ECMAScript
// * https://tc39.es/ecma262/

// Primitive Datatypes
let name = 'Geet' // string
let age = 21 // number
let isHappy = true // boolean
let negativity = null // null
let Goals // undefined
// `null` is a standalone value in JavaScript whereas `undefined` is used when value is not assigned
// `symbol` is used when we want to make something *unique*

console.log(typeof null) // Output: object
console.log(typeof undefined) // Output: undefined
